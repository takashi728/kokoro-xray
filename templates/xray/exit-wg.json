{
  "log": {
    "loglevel": "warning"
  },
  "inbounds": [
    {
      "tag": "WG_EXIT_IN",
      "listen": "0.0.0.0",
      "port": {{EXIT_WG_PORT}},
      "protocol": "wireguard",
      "settings": {
        "secretKey": "{{WG_PRIVKEY}}",
        "mtu": 1420,
        "peers": [
          {
            "publicKey": "{{PEER_PUBKEY}}",
            "allowedIPs": ["{{PEER_WG_IP}}/32"]
          }
        ]
      }
    }
  ],
  "outbounds": [
    {
      "tag": "DIRECT",
      "protocol": "freedom"
    },
    {
      "tag": "BLOCK",
      "protocol": "blackhole"
    }
  ],
  "routing": {
    "domainStrategy": "IPIfNonMatch",
    "rules": [
      {
        "type": "field",
        "ip": ["geoip:private"],
        "outboundTag": "BLOCK"
      }
    ]
  },
  "policy": {
    "levels": {
      "0": {
        "handshake": 2,
        "connIdle": 120
      }
    }
  }
}
